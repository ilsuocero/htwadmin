export const usePathsForm = (editMode, setEditMODE,
    pathForm,
    setPathForm,
    setPathNetwork,
    map,
    mapContainer,
    snap1,
    snap2,
    connectionState) => {
    useEffect(() => {

    }, [pathForm.show, pathForm.coordinates]);
}